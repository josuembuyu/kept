/**
 * Runs the evaluation set and prints the metrics table.
 * See docs/EVALUATION.md — the set is 20 hand-annotated transcripts, and the
 * blocking metric is the false `kept` rate.
 */
console.log("The evaluation set is not populated yet. See docs/EVALUATION.md.");
